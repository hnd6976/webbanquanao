@model IEnumerable<WebBanQuanAo.Models.Role>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="content-wrapper">
    <div class="page-header">
        <div class="header-sub-title">
            <nav class="breadcrumb breadcrumb-dash">
                <a asp-area="Admin" asp-controller="Home" asp-action="Index" class="breadcrumb-item"><i class="mdi mdi-home" style='color:blueviolet'></i></a>
                <span class="breadcrumb-item active">Quyền truy cập</span>
            </nav>
        </div>
    </div>
    <div class="page-header">
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <a class="btn btn-inverse-primary btn-fw btn-md" asp-action="Create"><i class="mdi mdi-folder-plus btn-icon-append"></i>  Tạo mới</a>
                    <table class="table table-bordered">
                        <thead style="color: black">
                            <tr>
                                <th scope="col">ID</th>
                                <th scope="col">Vai trò</th>
                                <th scope="col">Mô tả</th>
                                <th scope="col">#</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (Model != null)
                            {
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <th scope="row">@item.RoleId</th>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.RoleName)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.Description)
                                        </td>

                                        <td style="text-align:center">
                                            <a class="btn btn-inverse-primary m-r-5" asp-action="Edit" asp-route-id="@item.RoleId"><i class="mdi mdi-table-edit"></i></a> |
                                            <a class="btn btn-inverse-success m-r-5" asp-action="Details" asp-route-id="@item.RoleId"><i class="mdi mdi-file-document-box"></i></a> |
                                            <a class="btn btn-inverse-danger m-r-5" asp-action="Delete" asp-route-id="@item.RoleId"> <i class="mdi mdi-delete"></i></a>
                                         </td>
                                    </tr>
                                }
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>


